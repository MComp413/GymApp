const Fonts = Object.freeze({
  default: "Roboto",
  strong: "Poppins"
});

export const FontSizes = Object.freeze({
  tiny: 14,
  small: 18,
  medium: 24,
  large: 28,
  extraLarge: 32
});

export default Fonts;